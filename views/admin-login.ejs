<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Acceso administrativo | Gyminfinity</title>
    <meta
      name="description"
      content="Acceso administrativo de Gyminfinity con autenticacion por sesion y panel seguro."
    />
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <header class="site-header">
      <a href="/" class="brand">
        <img src="/images/logo.svg" alt="Logo de Gyminfinity" class="logo" />
        <div>
          <span class="brand-kicker">Gyminfinity Admin</span>
          <p>Ingreso para revisar la administracion del proyecto.</p>
        </div>
      </a>

      <nav class="nav-bar nav-bar-static" aria-label="Accesos">
        <a href="/">Volver al inicio</a>
        <a href="/client-login" class="btn btn-secondary nav-link">Area cliente</a>
      </nav>
    </header>

    <main class="auth-layout">
      <% if (flash) { %>
        <section class="status-messages">
          <div class="alert alert-<%= flash.type %>"><%= flash.text %></div>
        </section>
      <% } %>

      <section class="auth-card auth-intro">
        <span class="eyebrow">Panel administrativo</span>
        <h1>Entramos con usuario y contrasena para cuidar mejor el panel.</h1>
        <p>
          Esta parte nos permite manejar productos, usuarios, pedidos y membresias desde un solo lugar.
          La idea es que el panel quede claro para revisarlo y seguirlo mejorando.
        </p>

        <div class="feature-list">
          <article class="feature-row">
            <strong>Acceso cuidado</strong>
            <span>El panel se abre con credenciales y mantiene una sesion activa.</span>
          </article>
          <article class="feature-row">
            <strong>Informacion reunida</strong>
            <span>Productos, planes, rutinas, dietas, renovaciones y pedidos quedan en un mismo lugar.</span>
          </article>
          <article class="feature-row">
            <strong>Mejoras pendientes</strong>
            <span>Desde aqui podemos revisar que funciona y que debemos ajustar despues.</span>
          </article>
        </div>
      </section>

      <section class="auth-card">
        <div class="card-heading">
          <span class="section-kicker">Ingresar</span>
          <h2>Acceso administrativo</h2>
        </div>

        <form action="/admin-login" method="POST" class="form-shell">
          <input type="hidden" name="csrfToken" value="<%= csrfToken %>" />

          <div class="field">
            <label for="admin-username">Usuario</label>
            <input
              id="admin-username"
              type="text"
              name="username"
              maxlength="60"
              autocomplete="username"
              placeholder="admin"
              required
            />
          </div>

          <div class="field">
            <label for="admin-password">Contrasena</label>
            <div class="password-control">
              <input
                id="admin-password"
                type="password"
                name="password"
                maxlength="128"
                autocomplete="current-password"
                placeholder="Ingresa tu contrasena"
                required
              />
              <button
                type="button"
                class="password-toggle"
                data-password-toggle
                aria-controls="admin-password"
                aria-label="Mostrar contrasena"
              >
                Ver
              </button>
            </div>
          </div>

          <button type="submit" class="btn btn-primary btn-block">Entrar al panel</button>
          <p class="form-note">Usa las credenciales indicadas para revisar el panel de entrega.</p>
        </form>
      </section>
    </main>

    <footer class="site-footer">
      <p>&copy; <%= currentYear %> Gyminfinity. Proyecto academico en mejora continua.</p>
    </footer>

    <script src="/js/main.js"></script>
  </body>
</html>
